export const getCartItemCount = (state) => state.cart.length;

export const getUploadItemCount = (state) => state.uplodedProducts.length;

export const getItemQuentity = (state) => state.cart.quantity;

